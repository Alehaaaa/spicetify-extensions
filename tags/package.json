{
  "name": "tags",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "spicetify-creator",
    "build-local": "spicetify-creator --out=dist --minify",
    "build-github": "npm run build-local && npm run copy",
    "watch": "spicetify-creator --watch",
    "copy": "node scripts/extensions-github.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "shx": "^0.4.0",
    "spicetify-creator": "^1.0.17"
  },
  "dependencies": {
    "react-dom": "^19.1.0"
  }
}
